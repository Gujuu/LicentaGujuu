# Server
NODE_ENV=development
PORT=5000

# CORS (comma-separated allowlist). For local dev with Vite on 8080:
# CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=app_user
DB_PASSWORD=app_password
DB_NAME=restaurant_db


# Auth (JWT)
# Use a long random secret (at least 32+ chars)
JWT_SECRET=YOUR_LONG_RANDOM_JWT_SECRET
JWT_EXPIRE=7d

# Media storage
# Choose where uploaded images go: s3 | local
STORAGE_DRIVER=s3

# AWS S3
AWS_REGION=eu-central-1
S3_BUCKET_NAME=deifrati-bucket-s3
AWS_ACCESS_KEY_ID=AKIAZ33LBJCMHDOOMOOD
AWS_SECRET_ACCESS_KEY=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
# Optional (only if you use temporary credentials):
AWS_SESSION_TOKEN=

# Prefix inside the bucket (keeps it organized)
S3_PREFIX=media

